(function() {
  window.onload = function() {
    init();
  };
  var init = function() {
    // here we write the initialisation code
    // like openlayers.init() or map.init()
    initEvents();
  };
  var initEvents = function() {
    // all onclick (and other) event handlers should move here from index.html
  };
})();
